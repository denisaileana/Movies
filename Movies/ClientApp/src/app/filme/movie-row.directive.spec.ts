import { MovieRowDirective } from './movie-row.directive';

describe('MovieRowDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieRowDirective();
    expect(directive).toBeTruthy();
  });
});
